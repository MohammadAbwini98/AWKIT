# Superseded Remediation Prompt

The requested scope includes three validation campaigns, not only three application surfaces.

Use `FULL_VALIDATION_REMEDIATION_PROMPT.md`, which covers:

1. the comprehensive end-to-end validation suite;
2. Oracle JDBC and persisted row-driven workflow validation;
3. Recorder, System Reports, and Settings validation.
